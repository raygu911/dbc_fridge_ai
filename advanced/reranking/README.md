# Advanced Module — Reranking

Add a second-stage reranker after initial retrieval to improve the ordering of candidate recipes before prompt construction.

Planned topics include cross-encoder models, candidate-set sizing, latency and quality tradeoffs, score thresholds, caching, and offline ranking evaluation.
